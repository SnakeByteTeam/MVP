import { CheckCredentialsAdapter } from './check-credentials-adapter';

describe('CheckCredentialsAdapter', () => {
  it('should be defined', () => {
    expect(new CheckCredentialsAdapter()).toBeDefined();
  });
});
