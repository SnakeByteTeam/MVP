import { AlarmsRepositoryImpl } from './alarm-rules-repository-impl';

describe('AlarmsRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new AlarmsRepositoryImpl()).toBeDefined();
  });
});
