import { ExtractFromAccessTokenAdapter } from './extract-from-access-token-adapter';

describe('ExtractFromAccessTokenAdapter', () => {
  it('should be defined', () => {
    expect(new ExtractFromAccessTokenAdapter()).toBeDefined();
  });
});
