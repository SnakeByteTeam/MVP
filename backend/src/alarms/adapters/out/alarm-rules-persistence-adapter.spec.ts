import { AlarmRulesPersistenceAdapter } from './alarm-rules-persistence-adapter';

describe('AlarmRulesPersistenceAdapter', () => {
  it('should be defined', () => {
    expect(new AlarmRulesPersistenceAdapter()).toBeDefined();
  });
});
