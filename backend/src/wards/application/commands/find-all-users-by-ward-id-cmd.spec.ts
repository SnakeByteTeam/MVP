import { FindAllUsersByWardIdCmd } from './find-all-users-by-ward-id-cmd';

describe('FindAllUsersByWardIdCmd', () => {
  it('should be defined', () => {
    expect(new FindAllUsersByWardIdCmd()).toBeDefined();
  });
});
