import { ActiveAlarmsRepositoryImpl } from './active-alarms-repository-impl';

describe('ActiveAlarmsRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new ActiveAlarmsRepositoryImpl()).toBeDefined();
  });
});
