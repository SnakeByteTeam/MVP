import { CheckCredentialsCmd } from './check-credentials-cmd';

describe('CheckCredentialsCmd', () => {
  it('should be defined', () => {
    expect(new CheckCredentialsCmd()).toBeDefined();
  });
});
