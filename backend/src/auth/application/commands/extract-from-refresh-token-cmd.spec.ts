import { ExtractFromRefreshTokenCmd } from './extract-from-refresh-token-cmd';

describe('ExtractFromRefreshTokenCmd', () => {
  it('should be defined', () => {
    expect(new ExtractFromRefreshTokenCmd('')).toBeDefined();
  });
});
