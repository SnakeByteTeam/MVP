import { RemovePlantFromWardAdapter } from './remove-plant-from-ward-adapter';

describe('RemovePlantFromWardAdapter', () => {
  it('should be defined', () => {
    expect(new RemovePlantFromWardAdapter()).toBeDefined();
  });
});
