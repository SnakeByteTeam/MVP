import { FindAllWardsAdapter } from './find-all-wards-adapter';

describe('FindAllWardsAdapter', () => {
  it('should be defined', () => {
    expect(new FindAllWardsAdapter()).toBeDefined();
  });
});
