import { GetAllAlarmEventsAdapter } from './get-all-alarm-events-adapter';

describe('GetAllAlarmEventsAdapter', () => {
  it('should be defined', () => {
    expect(GetAllAlarmEventsAdapter).toBeDefined();
  });
});
