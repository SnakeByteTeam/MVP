import { GetAllAlarmEventsResDto } from './get-all-alarm-events-res-dto';

describe('GetAllAlarmEventsResDto', () => {
  it('should be defined', () => {
    expect(new GetAllAlarmEventsResDto()).toBeDefined();
  });
});
