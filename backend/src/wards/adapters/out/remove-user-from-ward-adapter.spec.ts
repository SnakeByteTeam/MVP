import { RemoveUserFromWardAdapter } from './remove-user-from-ward-adapter';

describe('RemoveUserFromWardAdapter', () => {
  it('should be defined', () => {
    expect(new RemoveUserFromWardAdapter()).toBeDefined();
  });
});
