import { FindAllAvailableUsersResDto } from './find-all-available-users-res-dto';

describe('FindAllAvailableUsersResDto', () => {
  it('should be defined', () => {
    expect(new FindAllAvailableUsersResDto()).toBeDefined();
  });
});
