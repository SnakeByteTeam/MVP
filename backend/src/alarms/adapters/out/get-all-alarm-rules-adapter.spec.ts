import { GetAllAlarmRulesAdapter } from './get-all-alarm-rules-adapter';

describe('GetAllAlarmRulesAdapter', () => {
  it('should be defined', () => {
    expect(new GetAllAlarmRulesAdapter({} as any)).toBeDefined();
  });
});
