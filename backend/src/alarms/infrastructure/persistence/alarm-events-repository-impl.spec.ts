import { AlarmEventsRepositoryImpl } from './alarm-events-repository-impl';

describe('AlarmEventsRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new AlarmEventsRepositoryImpl()).toBeDefined();
  });
});
