import { CheckFirstAccessCmd } from './check-first-access-cmd';

describe('CheckFirstAccessCmd', () => {
  it('should be defined', () => {
    expect(new CheckFirstAccessCmd('')).toBeDefined();
  });
});
