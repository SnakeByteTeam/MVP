import { WardsRepositoryImpl } from './wards-repository-impl';

describe('WardsRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new WardsRepositoryImpl()).toBeDefined();
  });
});
