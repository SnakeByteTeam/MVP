import { ExtractFromRefreshTokenAdapter } from './extract-from-refresh-token-adapter';

describe('ExtractFromRefreshTokenAdapter', () => {
  it('should be defined', () => {
    expect(new ExtractFromRefreshTokenAdapter()).toBeDefined();
  });
});
