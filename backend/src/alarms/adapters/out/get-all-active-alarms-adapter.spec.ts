import { GetAllActiveAlarmsAdapter } from './get-all-active-alarms-adapter';

describe('GetAllActiveAlarmsAdapter', () => {
  it('should be defined', () => {
    expect(new GetAllActiveAlarmsAdapter()).toBeDefined();
  });
});
