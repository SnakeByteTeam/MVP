import { CheckFirstAccessAdapter } from './change-credentials-adapter';

describe('CheckFirstAccessAdapter', () => {
  it('should be defined', () => {
    expect(new CheckFirstAccessAdapter()).toBeDefined();
  });
});
