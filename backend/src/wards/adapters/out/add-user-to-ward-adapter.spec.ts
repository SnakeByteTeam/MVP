import { AddUserToWardAdapter } from './add-user-to-ward-adapter';

describe('AddUserToWardAdapter', () => {
  it('should be defined', () => {
    expect(new AddUserToWardAdapter()).toBeDefined();
  });
});
