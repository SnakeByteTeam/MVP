import { Component } from '@angular/core';
import { IDeviceCard } from '../../interfaces/device-card.interface';

@Component({ selector: 'app-blind-card', standalone: true, template: '' })
export class BlindCardComponent implements IDeviceCard {}
