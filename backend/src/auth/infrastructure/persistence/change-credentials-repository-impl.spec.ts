import { CheckFirstAccessRepositoryImpl } from './change-credentials-repository-impl';

describe('CheckFirstAccessRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new CheckFirstAccessRepositoryImpl()).toBeDefined();
  });
});
